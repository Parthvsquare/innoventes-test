version: "3.8"

services:
  psql:
    image: postgres:latest
    environment:
      POSTGRES_DB: juckbox
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: qwert@12345
    ports:
      - "5432:5432"
    volumes:
      - psql_volume:/var/lib/postgresql/data

volumes:
  psql_volume:
